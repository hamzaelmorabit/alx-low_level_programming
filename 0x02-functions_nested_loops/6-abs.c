#include "main.h"
/**
 * _abs - entry point
 * Description: computes the absolute value of an integer.
 * Return: absolute value
 * @i: Ascii code
 */
int _abs(int i)
{
if (i < 0)
{
return (-i);
}
return (i);
}
